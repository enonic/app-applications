# ----------------------------------------------------------------------------
# Admin extension descriptor
# Resolved by XP from applications.yaml, not by the UI: the shell renders the
# rail from the localized title the platform hands it.
# ----------------------------------------------------------------------------

section.applications.title = Applications
section.applications.description = Install, update and manage applications

# ----------------------------------------------------------------------------
# Applications section
# Read by the section itself, through the `phrases(locale)` root field.
# ----------------------------------------------------------------------------

applications.heading = Applications
